# MasVis for Windows application icon

`masvis-for-windows.png` is the runtime/project artwork.
`masvis-for-windows.ico` is the multi-size Windows icon intended for the packaged executable and Windows shell integration.

The app icon is separate from the Microsoft Fluent UI System Icons used for toolbar/action glyphs.
